DB_HOST=localhost
DB_PORT=5432
DB_NAME=autopro
DB_USER=autopro
DB_PASSWORD=autopro
PORT=3000
JWT_SECRET=change-me-in-production

# Auth (set JWT_SECRET in production)
JWT_SECRET=change-me-to-a-long-random-string
BCRYPT_ROUNDS=10

# Seeded on first run if users table is empty (override in production)
INIT_MANAGER_EMPLOYEE_ID=manager1
INIT_MANAGER_PASSWORD=password123
INIT_ASSISTANT_EMPLOYEE_ID=mech1
INIT_ASSISTANT_PASSWORD=password123
